# muto-wiki — 위키 관리 규칙 (Schema)

이 저장소는 muto의 개인 지식 위키다. Andrej Karpathy의 [LLM Wiki 패턴](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)을 따른다.

**이 파일이 유일한 규칙 원본이다.** Claude Code는 `CLAUDE.md`로, Codex 등 다른 에이전트는 `AGENTS.md`(이 파일의 심볼릭 링크)로 같은 내용을 읽는다. 규칙을 고칠 때는 이 파일만 고친다.

`README.md`는 **사람이 읽는 사용설명서**이지 규칙 원본이 아니다. 규칙이 바뀌면 이 파일을 고치고, `README.md`에서 사용법 설명에 영향이 가는 부분만 따라 갱신한다. 규칙 전문을 `README.md`에 옮겨 적지 않는다 — 두 파일이 갈라진다.

---

## 0. 사용자 컨텍스트 — 모든 판단의 기준

> 이 절은 위키의 **방향타**다. Takeaway를 쓸 때, 어떤 자료를 어느 페이지에 넣을지 정할 때,
> 무엇을 버릴지 정할 때 항상 이 절을 기준으로 판단한다.

- **직무**: IT 기업 DBA / DBRE
- **하는 일**: DB 운영, 그리고 **DB 운영을 자동화하는 툴·에이전트 개발**
- **다루는 엔진**: MySQL/Aurora MySQL, PostgreSQL, SQL Server (3사 대조가 기본값)
- **방향성**: 사람이 반복하던 운영 판단을 **자동화 가능한 규칙으로 환원**하는 것.
  지식은 "알아두면 좋은 것"이 아니라 **런북·점검 항목·에이전트 로직으로 옮길 수 있는 형태**여야 가치가 있다.

그래서 모든 페이지의 첫 블록은 "이 내용이 내 운영/자동화 업무에서 **무엇을 바꾸는가**"를 답해야 한다.

---

## 1. 구조

```
muto-wiki/
├── README.md              사람이 읽는 사용설명서. 규칙 원본이 아니다
├── CLAUDE.md              이 파일 — 스키마 (실체)
├── AGENTS.md              → CLAUDE.md 심볼릭 링크 (Codex·기타 에이전트용)
├── scripts/
│   ├── sync.sh            git 동기화 (원격 우선)
│   ├── notion-convert.py  위키 마크다운 → Notion 마크다운 (§6.4)
│   └── notion-stamp.py    notion_page_id·notion_synced 기록 (§6.3)
├── .claude/settings.json  Claude Code 훅 — 세션 시작 pull / 턴 종료 push
├── .codex/hooks.json      Codex 훅 — 같은 동작
├── raw/                   원본 자료 — 사용자가 넣고, 에이전트는 읽기만
│   └── archive/           위키 승격이 끝난 원본
└── wiki/                  정리된 지식 — 에이전트가 작성·관리
    ├── index.md           전체 목차 (카테고리별)
    ├── log.md             작업 이력 (append-only)
    └── *.md               위키 페이지 (하위 폴더 없이 플랫)
```

**`wiki/`는 절대 하위 폴더를 만들지 않는다.** 분류는 오직 `index.md`의 카테고리 절로 표현한다.
폴더가 없으므로 wikilink는 항상 `[[페이지명]]` 형태이며 경로 접두어가 붙지 않는다.

### 파일명 규칙

- 영문 kebab-case, 확장자 `.md` — 예: `mysql-operations.md`
- 내용을 보고 짐작 가능한 이름. 날짜·번호만으로 된 이름 금지
- 플랫 구조라 이름이 곧 네임스페이스다. 충돌 위험이 있으면 한정어를 앞에 붙인다
  - 연도 업무 로그: `worklog-<회사>-<연도>.md` (예: `worklog-kakaogames-2026.md`)
  - 특정 제품 한정 지식: `<제품>-<주제>.md`

---

## 2. 페이지 형식

### 2.1 골격

````markdown
---
title: MySQL/Aurora MySQL 운영 지식
category: db운영
tags: [mysql, aurora, backup]
summary: 200자 이내 한 줄 요약. 페이지를 열지 않고도 무슨 내용인지 알 수 있게.
sources: [사내 강의 2026-07, MySQL 8.0 릴리스 노트]
status: verified
created: 2026-08-04
updated: 2026-08-15
notion_page_id: null
notion_synced: null
---

> [!tip] 핵심 Takeaway
> - 회수 릴리스(8.0.29, 8.0.38/8.4.1)는 업그레이드 후보에서 즉시 제외 — 버전 선정 자동화의 하드 필터로 박아둘 것
> - 복구 테스트 없는 백업은 백업이 아니다. 주간 점검 에이전트의 1순위 항목

# MySQL/Aurora MySQL 운영 지식

한 문단 개요.

## 소제목

본문. 관련 지식은 [[operational-queries]], [[aurora-dsql]] 처럼 교차참조한다.

## Sources

- 출처 목록
````

### 2.2 첫 블록은 **반드시** Takeaway callout

**이것이 이 위키의 가장 중요한 규칙이다.** 모든 페이지는 `---` frontmatter 바로 다음,
`#` 제목보다도 **먼저** 아래 블록으로 시작한다:

```markdown
> [!tip] 핵심 Takeaway
> - (한 줄)
> - (한 줄)
```

- **2~4줄.** 그 이상이면 요약이 아니라 본문이다
- **내용 요약이 아니라 행동 지침으로 쓴다.** §0의 미션·방향성 관점에서
  "그래서 내가 뭘 해야/하지 말아야 하는가"를 답한다
- 판단·기준·임계값·금지사항처럼 **자동화 규칙으로 옮길 수 있는 형태**를 우선한다
- 페이지를 갱신하면 Takeaway도 같이 재검토한다. 본문만 바뀌고 Takeaway가 낡는 것을 막는다

| ❌ 나쁜 예 (요약) | ✅ 좋은 예 (행동 지침) |
|---|---|
| MySQL 8.0.42에 파티션 pruning 버그가 있다 | 8.0.42는 증상이 없어도 안전한 게 아니다 — INSERT 이력이 없어 미노출일 뿐. 업그레이드 게이트에서 차단 |
| Aurora DSQL은 분산 SQL 데이터베이스다 | DSQL은 스토리지 무제한이 아님(클러스터당 10 TiB 기본). 용량 산정 시 이 한도부터 확인 |

### 2.3 frontmatter 필드

| 필드 | 필수 | 설명 |
|---|---|---|
| `title` | ✅ | 한국어 제목 |
| `category` | ✅ | `db운영` / `업무기록` / `개인` / `참고자료` / `종합원칙` 중 하나 (§3) |
| `tags` | ✅ | 소문자 영문 배열 |
| `summary` | ✅ | 200자 이내. `index.md` 항목과 질문 답변의 1차 탐색 근거 |
| `sources` | ✅ | 근거 출처. 없으면 `[]`가 아니라 왜 없는지 적는다 |
| `status` | ✅ | `draft` / `reviewed` / `verified` / `disputed` / `archived` (§2.5) |
| `created` / `updated` | ✅ | ISO 날짜. 상대 날짜("지난주")는 절대 날짜로 변환해 기록 |
| `notion_page_id` | ✅ | Notion 동기화 전에는 `null` |
| `notion_synced` | ✅ | 마지막 Notion push 시각. 미동기화 시 `null` |
| `due` | — | 일정이 있는 페이지만. ISO 날짜 |

### 2.4 출처 표시

기본은 "자료가 실제로 말한 것"이다. 그렇지 않은 문장만 표시한다.

| 상태 | 표기 | 의미 |
|---|---|---|
| 추출 | *(표기 없음)* | 자료가 실제로 말한 내용의 재서술 |
| 추론 | 문장 끝에 `^[inferred]` | 자료가 직접 말하지 않은 연결·일반화 |
| 상충 | 문장 끝에 `^[ambiguous]` | 자료끼리 다르거나 원문이 모호함 |

### 2.5 status 승격 규칙

- 새로 만든 페이지는 언제나 `draft`
- **구술·사내 자료 출처는 공식 문서와 대조하기 전까지 `verified`로 승격하지 않는다.**
  기존 볼트에서 실제 오류 5건이 나온 규칙이다. 오류는 **수치·버전·한도**에 몰린다 →
  [[verbal-source-verification-policy]]
- `verified` / `reviewed` 승격은 사용자 확인을 받고 한다. 에이전트가 임의로 올리지 않는다
- 대체된 페이지는 `archived` + `superseded_by: "[[새페이지]]"`

---

## 3. 카테고리와 적재 규칙

폴더가 아니라 `category` frontmatter와 `index.md` 절로만 분류한다.

| category | 무엇이 들어가나 | 예 |
|---|---|---|
| `db운영` | DB 설정값·쿼리·제품 분석·버전/버그 리포트. **회사 맥락을 최소화한 재사용 가능 지식** | `mysql-operations`, `aurora-dsql` |
| `업무기록` | 회사에서 수행한 트러블슈팅·성능개선·강의 정리·일/주/월 업무 로그. **언제, 왜, 내가 무엇을 했고, 임팩트가 무엇인지** | `worklog-kakaogames-2026` |
| `개인` | 일정·할 일·관심 주식·부동산 등 업무 지식이 아닌 것 | `todo` |
| `참고자료` | 외부 소스(문서·저장소·글) 하나를 정리한 페이지 | `superpowers-agentic-development-methodology` |
| `종합원칙` | 여러 자료·경험을 관통하는 판단 기준. 위키가 스스로 학습한 결론 | `verbal-source-verification-policy` |

### 3.1 지식과 업무 기록을 한 페이지에 섞지 않는다

하나의 사건이 양쪽에 해당하면 **페이지를 나눈다**:

1. `db운영` 페이지 — 재사용 가능한 기술 지식만
2. `업무기록` 페이지 — 시점, 배경, 내가 한 일, 임팩트
3. 둘을 `[[wikilink]]`로 **상호** 연결

이렇게 해야 `db운영`은 이직 후에도 쓸 수 있는 지식베이스가 되고,
`업무기록`은 그대로 성과 정리·포트폴리오 원천이 된다.

### 3.2 업무 로그 누적 규칙

일/주/월 단위 업무 정리는 페이지를 남발하지 않는다.

- 연도별 페이지 하나(`worklog-<회사>-<연도>.md`)에 월 단위 절로 누적한다
- 그 안에서 특정 건이 독립 페이지로 클 만큼 커지면 그때 분리하고 `[[링크]]`로 잇는다

### 3.3 단발성 할 일

날짜 있는 단발성 일정·할 일은 페이지를 만들지 않고 `todo.md` 한 페이지에 체크박스로 누적한다.
반복 일정이나 개인 프로젝트 계획은 건별 페이지로 만든다.

---

## 4. 교차참조 규칙

**새 페이지는 관련 페이지 최소 2개와 연결한다.** 링크 없는 페이지는 검색으로만 닿을 수 있어
위키가 아니라 파일 더미가 된다.

- 문법: `[[페이지명]]` 또는 `[[페이지명|표시할 텍스트]]`. 경로 접두어(`dba/`)를 붙이지 않는다
- **단방향 링크 금지.** A에서 B를 걸었으면 B의 본문에도 A로 돌아오는 맥락 있는 링크를 넣는다
- 링크는 문장 안에 자연스럽게 넣는다. 페이지 끝에 "관련 문서" 목록만 나열하는 것으로 갈음하지 않는다
- 페이지를 만들거나 고칠 때마다 **기존 페이지 중 이 페이지를 링크해야 할 곳**을 찾아 함께 갱신한다

---

## 5. 세 가지 운영 방법

### 5.1 자료 넣기 (INGEST)

**트리거**: `raw/`에 파일을 넣고 "자료 넣었어" / 대화에 내용을 붙여넣기 / 링크 공유

**절차**:

1. **읽는다** — `raw/`의 미처리 파일 또는 붙여넣은 내용. `raw/`는 절대 수정하지 않는다
2. **기존 페이지를 먼저 찾는다** — `index.md`와 `summary`를 훑어 관련 페이지를 **모두** 찾는다.
   여기가 핵심이다. 새 페이지를 만드는 것은 **마지막 수단**이고, 기본은 **기존 페이지에 병합**이다
3. **§3 라우팅으로 분류한다** — 지식과 업무 기록이 섞여 있으면 나눈다
4. **쓴다** — 관련 페이지를 전부 갱신한다. 요약을 하나 더 만드는 게 아니라 지식을 흡수시킨다
   - 기존 내용과 **모순되면 덮어쓰지 않고** 양쪽을 남긴 뒤 `^[ambiguous]` 표시하고 사용자에게 보고
   - **Takeaway callout을 재작성한다** (§2.2)
   - `updated` 갱신
5. **교차참조를 연결한다** (§4) — 새 페이지뿐 아니라 링크해야 할 기존 페이지도 함께 고친다
6. **`index.md`를 갱신한다** — 신규/변경 항목 반영
7. **`log.md`에 기록한다** (§7)
8. **원본을 옮긴다** — `raw/`의 파일은 `raw/archive/`로 이동한다. **삭제하지 않는다**

### 5.2 질문하기 (QUERY)

**트리거**: 그냥 질문한다

**절차** — 싼 것부터 쓰고, 부족할 때만 다음 단계로 올라간다:

| 알고 싶은 것 | 수단 | 비용 |
|---|---|---|
| 그런 페이지가 있나 / 제목·태그 | `wiki/index.md` 읽기 | 최저 |
| 페이지 대략의 내용 | frontmatter `summary` | 낮음 |
| 특정 주장·구절 | `rg -A 5 -B 2 "<검색어>" wiki/` | 중간 |
| 페이지 전체 | `Read` | 높음 — 최후 수단 |

**답변 규칙**:

- 근거가 된 페이지를 `[[페이지명]]`으로 인용한다. 어느 페이지에서 왔는지 알 수 없는 답변은 만들지 않는다
- 위키에 없으면 **없다고 먼저 말한다.** 일반 지식으로 답할 때는 "위키에 없는 내용"임을 명시한다
- `status: draft`인 페이지를 근거로 쓸 때는 미검증임을 밝힌다
- 답변 과정에서 나온 결론이 **재사용 가치가 있으면 페이지로 환원한다**(자료 넣기와 동일 절차).
  질문이 위키를 키우게 하는 것이 이 패턴의 핵심이다
- `log.md`에 `QUERY` 기록

### 5.3 건강검진 (LINT)

**트리거**: "건강검진해줘"

전수 점검 후 **항목별 리포트를 먼저 내고, 승인받고 고친다.** 임의로 고치지 않는다.

| 점검 항목 | 판정 기준 |
|---|---|
| 깨진 wikilink | 대상 페이지가 없는 `[[링크]]` |
| 고아 페이지 | 들어오는 링크가 0개인 페이지 (`index.md` 제외) |
| 단방향 링크 | A→B는 있는데 B→A가 없는 쌍 (§4 위반) |
| Takeaway 누락·노후 | 첫 블록이 callout이 아니거나, 본문 대비 Takeaway가 낡음 |
| frontmatter 누락 | §2.3 필수 필드 결손 |
| stale | `updated`가 90일 초과 |
| 모순 | 페이지 간 수치·버전·한도가 어긋남 (가장 중요) |
| index 불일치 | `wiki/*.md`와 `index.md` 항목 집합이 다름 |
| 미처리 원본 | `raw/`에 남아 있는데 대응 위키 내용이 없는 파일 |
| Notion 지연 | `updated > notion_synced` 인 페이지 |
| 승격 후보 | `draft`인데 공식 출처 대조가 끝난 페이지 |
| 민감정보 | 계정·비밀번호·토큰·접속 문자열·내부 IP·고객 데이터 흔적 (발견 즉시 보고) |

`log.md`에 `LINT` 기록.

---

## 6. Notion 동기화

**단방향(`wiki/` → Notion)이며, 명시적으로 요청할 때만 실행한다.** wiki가 항상 진실이다.

**트리거**: "노션 동기화해줘"

### 대상 (확정 — 2026-08-15)

**부모 페이지: `DBA`** — https://www.notion.so/DBA-3aefb969b8be801280b8dc2ff35fbefb
(page_id `3aefb969b8be801280b8dc2ff35fbefb`)

**다시 묻지 않는다.** 사용자가 명시적으로 다른 페이지를 지정할 때만 바꾼다.
이 페이지 아래의 `기술 문서`는 위키가 만든 것이 아닌 **기존 자료**다 — 건드리지 않는다.

**동기화 제외**: `index.md`, `log.md` (`category: 색인`).
목차는 Notion 페이지 트리가 대신하고, 작업 이력은 로컬 위키의 내부 기록이다.

**절차**:

1. 위 부모 페이지 아래에 §6.1 구조로 배치한다. 구조가 없으면 만든다
2. **§6.3 증분 판정**으로 대상을 고른다. 마지막 동기화 이후 바뀌지 않은 페이지는 **건너뛴다**
3. **§6.1 배치 규칙**으로 위치를 정하고, **§6.2 병합 규칙**으로 쓴다.
   Takeaway callout은 Notion callout 블록으로 옮긴다
4. **성공한 페이지마다 즉시 `notion_page_id`, `notion_synced`를 기록한다** (§6.3)
5. `log.md`에 `SYNC_NOTION` 기록 — 올린 수와 **건너뛴 수를 함께** 남긴다

**제약**: Notion MCP가 연결된 Claude 세션에서만 가능하다. Codex 등 MCP가 없는 환경에서는
동기화를 시도하지 말고 "Claude 세션에서 실행 필요"라고 안내한다.
Notion에서 직접 편집한 내용은 회수하지 않는다 — 편집은 항상 `wiki/`에서 한다.

### 6.1 배치 — 카테고리 아래를 한 단계 더 묶는다

`wiki/`는 플랫이지만 Notion에서는 `category`가 계층으로 펼쳐진다.
그중 `db운영`과 `업무기록`은 페이지가 많아지므로 **한 단계를 더 둔다.**

```
<부모 페이지>
├── db운영
│   ├── MySQL                  ← Aurora MySQL · RDS MySQL · Community MySQL 전부 여기
│   ├── PostgreSQL             ← Aurora PostgreSQL · RDS PostgreSQL · Community PG
│   ├── SQL Server             ← Azure SQL · RDS SQL Server 포함
│   ├── Aurora DSQL
│   └── 엔진 공통
│       ├── 엔진 비교          ← 3사 대조 개념, 클라우드 플랫폼 공통 지식
│       ├── 진단·운영 표준     ← 진단 쿼리, 모니터링, 장애 대응 절차
│       ├── 보안·권한          ← 계정·권한 설계, 감사 체크리스트
│       ├── 개발·자동화        ← 툴링 표준, 에이전트·봇
│       └── 지식 운영          ← 위키·포털 거버넌스
├── 업무기록
│   └── kakaogames
├── 개인
├── 참고자료
└── 종합원칙
```

**엔진 판정 기준** — 호스팅 형태가 아니라 **엔진 계보**로 판단한다.

- **관리형 변종은 원 엔진으로 묶는다.** Aurora MySQL·RDS MySQL·Community MySQL은
  운영 방법이 달라도 **같은 MySQL**이다. PostgreSQL·SQL Server도 같은 원칙
- **wire protocol 호환만으로는 같은 엔진이 아니다.** [[aurora-dsql]]은 PostgreSQL v3
  프로토콜을 쓰지만 내부가 PostgreSQL이 아니고 OCC·미지원 기능·한도가 전혀 다르다 →
  **별도 엔진으로 둔다**
- 두 엔진 이상을 **대등하게** 다루는 페이지는 `엔진 공통`. 특정 엔진이 주(主)이고
  나머지가 비교용이면 주 엔진 쪽에 둔다
- 새 엔진이 등장하면 하위 페이지를 만들기 전에 **사용자에게 확인받는다**

**`엔진 공통` 소분류** — [[index]]의 `db운영` 소절과 **이름을 맞춘다.**
두 곳이 갈라지면 같은 페이지가 목차와 Notion에서 서로 다른 곳에 놓여 찾을 수 없게 된다.

- 소분류를 바꾸면 `index.md`의 절도 **함께** 고친다
- 예외 1건 — `index.md`의 `엔진별 운영` 절은 Notion에서 **엔진 그룹 4개 + `엔진 비교`로 갈라진다.**
  목차는 엔진 수가 적어 한 절로 묶여 있고, Notion은 엔진별로 나누기 때문이다
- 소분류 어디에도 넣기 애매하면 새 소분류를 만들기 전에 **사용자에게 확인받는다.**
  `엔진 공통` 바로 아래에 페이지를 흘려두지 않는다

**회사 판정 기준** — `업무기록`은 회사별로 묶는다.

- **현재 회사는 `kakaogames`.** 회사 표기가 없는 기존 업무기록은 **전부 `kakaogames`로 귀속**한다
- 이직 시 새 회사 그룹을 만든다. **과거 회사 그룹은 그대로 둔다** — 옮기거나 합치지 않는다

### 6.2 병합 — 기존 페이지가 있으면 새로 만들지 않는다

**`notion_page_id`가 `null`이라고 해서 곧바로 새로 만들지 않는다.**
같은 내용의 페이지가 Notion에 이미 있으면 중복이 생긴다.

1. `notion_page_id`가 있으면 → 그 페이지를 갱신한다
2. `null`이면 → **배치 위치에서 같은 주제 페이지를 먼저 찾는다**
   - 제목이 일치하면 그 페이지에 병합하고 `notion_page_id`를 기록한다
   - 제목은 달라도 같은 주제로 보이면 **사용자에게 확인받는다.** 임의로 합치지 않는다
   - 없을 때만 새로 만든다

**병합 방식**:

- **wiki에도 있는 절은 wiki 내용으로 덮는다.** wiki가 진실이다
- **Notion에만 있는 절은 지우지 않고 남긴다.** 단 wiki로 회수하지는 않는다 (단방향 원칙)
- 남긴 절이 wiki 내용과 **모순되면 삭제하지 말고 사용자에게 보고한다** (§9-3과 같은 원칙)

> **한계** — 남겨둔 Notion 전용 절은 `wiki/`에 존재하지 않으므로 §5.3 건강검진의 모순·stale
> 점검이 닿지 않는다. 병합을 반복할수록 위키가 추적하지 못하는 내용이 Notion에 쌓인다.
> 살릴 값어치가 있는 절은 발견 시 보고하고 **`wiki/`로 옮겨 적는 것이 원칙**이다.

### 6.3 증분 판정 — 안 바뀐 페이지는 올리지 않는다

**대상 조건은 단 하나다.**

```
notion_synced 가 null      → 대상 (한 번도 안 올림)
updated > notion_synced    → 대상 (올린 뒤 수정됨)
그 외                       → 건너뛴다
```

- **전체 재업로드는 금지다.** "확실히 하려고 전부 다시 올린다"는 선택지는 없다 —
  Notion 쪽 수정 이력이 매번 뭉개지고, 무엇이 실제로 바뀌었는지 알 수 없게 된다
- 강제 재업로드가 필요하면 **사용자가 페이지를 명시**해야 한다. 에이전트가 판단하지 않는다

**기록은 페이지 단위로 즉시 한다.** 마지막에 한꺼번에 쓰지 않는다 —
중간에 실패하면 어디까지 올렸는지 알 수 없어 다음 실행이 전량 재업로드가 된다.

| 필드 | 값 |
|---|---|
| `notion_page_id` | 생성·병합된 Notion 페이지 ID (따옴표로 감싼 문자열) |
| `notion_synced` | 그 페이지를 올린 시각. `updated`와 같은 형식으로 비교 가능해야 한다 |

> **이 기록을 빠뜨리면 증분 판정 자체가 죽는다.** 전부 `null`로 남아 매 실행이
> 전량 재업로드가 된다. 2026-08-15 최초 동기화에서 실제로 13개 페이지를 올리고
> 기록을 빠뜨려 사후에 채워 넣었다 — 같은 실수를 반복하지 않는다.

### 6.4 실행 방법 — 비용 규칙

Notion MCP는 **본문을 tool 파라미터로 인라인 전달**해야 한다. 파일을 읽어 올리는 경로가
없으므로 모든 본문이 에이전트의 출력 토큰을 통과한다. 위키 전체가 약 230 KB이므로
아래 넷을 지키지 않으면 비용이 3배로 뛴다. **전부 2026-08-15 최초 동기화에서 실제로 겪은 것이다.**

**① JSON에 한글을 `\uXXXX`로 이스케이프하지 않는다.** 원시 UTF-8을 그대로 넣는다.
JSON은 원시 UTF-8을 받으므로 이스케이프는 불필요하고, 본문의 26%가 비ASCII라
이스케이프하면 **문자 수가 2.24배**(160 KB → 358 KB)로 부푼다. 가장 큰 낭비 요인이다.

**② tool 호출은 표준 파라미터 형식만 쓴다.** 원문 JSON을 통째로 밀어넣는 형식은
길이가 커지면 잘려 `InputValidationError`가 나고, 그때마다 페이지 하나를 통째로 다시 써야 한다.
최초 동기화에서 이걸로 **8회 실패·97 KB(코퍼스의 42%)·8턴**을 버렸다.
**한 번 실패하면 즉시 형식을 바꾼다. 같은 형식으로 재시도하지 않는다.**

**③ 같은 부모 아래 페이지는 한 번의 호출에 배열로 묶는다.** `notion-create-pages`는
페이지 배열을 받되 **부모가 같아야 한다.** §6.1 구조의 부모는 12개뿐이므로
31개 페이지도 12회 안에 끝난다 — 한 페이지씩 부르면 31회가 된다.

**④ 큰 페이지는 생성 후 이어붙인다.** 한 호출에 담기 어려운 페이지(수십 KB)는
`create-pages`로 앞부분을 만들고 `update-page`의 `insert_content`로 절 단위 추가한다.

**보조 스크립트** — 매번 새로 만들지 말고 저장소의 것을 쓴다.

| 스크립트 | 용도 |
|---|---|
| `scripts/notion-convert.py <파일>` | 위키 마크다운 → Notion 마크다운. **GFM 테이블 → `<table>` XML**, `> [!tip]` → `<callout>`, `[[링크]]`, `^[inferred]`, 들여쓰기 탭 변환. 코드블록 안은 건드리지 않는다 |
| `scripts/notion-stamp.py <시각> <slug>=<page_id> ...` | §6.3의 `notion_page_id`·`notion_synced` 일괄 기록 |

> **GFM 파이프 테이블은 Notion에서 표로 렌더링되지 않는다.** 반드시 `<table>` XML로 바꾼다 —
> 손으로 하면 페이지 수만큼 어긋나므로 변환기를 쓴다.

**교차참조** — 동기화 직후 위키링크는 인라인 코드로만 남는다. 모든 페이지가 생성돼
`notion_page_id`가 채워진 뒤 **2차 패스**로 `<mention-page url="..."/>`(self-closing —
제목은 Notion이 자동 표시하므로 불일치가 없다)로 바꾼다. 이때는 본문 전체가 아니라
`update-page`의 `content_updates`에 **치환 쌍만** 보낸다.

---

## 7. log.md 기록 형식

모든 쓰기 작업 후 `wiki/log.md` **맨 위에** 한 줄 추가한다 (최신이 위).

```
- [2026-08-15T15:40:00+09:00] INGEST source="raw/2026-08-15-aurora-upgrade.md" pages_created=1 pages_updated=3
- [2026-08-15T16:02:00+09:00] QUERY query="Aurora 업그레이드 시 확인 항목" result_pages=3
- [2026-08-15T17:00:00+09:00] LINT issues=4 broken_links=2 orphans=0 stale=1 contradictions=1
- [2026-08-15T18:00:00+09:00] SYNC_NOTION pages=5 target="muto-wiki"
- [2026-08-15T18:30:00+09:00] UPDATE page="mysql-operations" note="8.0.43 릴리스 노트 반영"
```

연산: `INGEST` / `QUERY` / `LINT` / `SYNC_NOTION` / `UPDATE` / `MIGRATE` / `FIX`

---

## 8. git 동기화

**세션을 열 때 최신을 내려받고, 마칠 때 올린다. 충돌 시 원격(origin/main)이 항상 우선한다.**

```bash
bash scripts/sync.sh pull   # 세션 시작 시
bash scripts/sync.sh push   # 작업 종료 시
```

- **Claude Code**: `.claude/settings.json` 훅이 자동 실행한다 (`SessionStart` → pull, `Stop` → push). 수동 실행 불필요
- **Codex**: `.codex/hooks.json` 훅이 같은 동작을 한다. 수동 실행 불필요.
  Claude와 달리 `$CLAUDE_PROJECT_DIR`이 없으므로 `$(git rev-parse --show-toplevel)`로 repo root를 찾는다
- **그 외 에이전트**: 훅이 없으므로 **직접 실행한다.**
  세션 시작 후 첫 작업 전에 `pull`, 위키 파일을 고친 뒤 `push`

### git 규칙

- **브랜치를 생성하지 않는다.** `git checkout -b`, `git switch -c`, `git branch <이름>` 금지
- 모든 커밋은 `main`에서 직접 한다. 개인 지식 저장소이며 PR 흐름이 없다
- 충돌은 원격 우선으로 해소한다. 다른 기기에서 넣은 내용이 로컬 임시 상태보다 우선한다

---

## 9. 안전 규칙

1. **`raw/`는 읽기 전용.** 수정·삭제하지 않는다. 승격 후 `raw/archive/`로 **이동**만 한다
2. **민감정보 금지.** 계정, 비밀번호, 토큰, 접속 문자열, 내부 IP, 고객 데이터는 어느 페이지에도
   기록하지 않는다. 필요하면 위치만 남긴다 (예: "자격증명은 사내 vault 참조").
   자료에 섞여 들어오면 해당 부분을 제외하고 정리한 뒤 사용자에게 알린다
3. **모순은 덮어쓰지 않는다.** 새 자료가 기존 내용과 어긋나면 양쪽을 남기고 `^[ambiguous]` 표시 후 보고
4. **사용자가 쓴 것을 지우지 않는다.** 사용자가 직접 편집한 문장·페이지는 병합 대상이지 삭제 대상이 아니다
5. **추측을 사실처럼 쓰지 않는다.** 모르면 `## Open Questions` 절에 남긴다

---

## 10. 핵심 원칙

1. **컴파일하되, 재검색하지 않는다.** 위키는 미리 정리된 지식이다. 자료를 넣으면 관련 페이지를
   **전부** 갱신한다. 자료 요약본을 하나 더 만드는 것이 아니다
2. **시간이 갈수록 똑똑해진다.** 매 적재가 위키를 크게만 하는 게 아니라 낫게 만들어야 한다 —
   병합하고, 모순을 해소하고, 교차참조를 강화한다
3. **출처가 남는다.** 모든 주장은 출처로 되짚을 수 있어야 한다
4. **추론은 표시한다.** 추측을 감추는 위키는 조용히 썩고, 표시하는 위키는 신뢰를 유지한다
5. **사람이 큐레이션하고, 에이전트가 유지한다.** 무엇을 넣고 무엇을 물을지는 사용자가 정한다.
   교차참조·일관성·목차 갱신 같은 관리는 에이전트가 한다
6. **Obsidian이 IDE다.** 저장소 루트가 그대로 Obsidian 볼트다. 항상 유효한 Obsidian 마크다운이어야 한다
